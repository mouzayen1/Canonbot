# Secrets (never commit these)
.env

# NOTE: config.yaml IS committed so CI / Render have your watch list.
# It contains no secrets — those live in .env / repo secrets.

# Runtime state
state.json
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/
